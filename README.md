# Mapping tool published objects

This repository receives design-time objects (mappings, routes, schemas)
published from the mapping tool's Source Control surface. This file was
created automatically to initialize the branch; published objects arrive
as one commit per explicit publish.
